# Changelog
## Phase 6

- Added consistent vault snapshots, versioned backup manifests, complete
  note/audio/attachment inclusion, SHA-256 validation, safe new-vault restore,
  collision handling, SQLite integrity checks, and recovery UI.
- Completed desktop settings navigation, accessibility and performance review,
  Windows Release/MSIX packaging, release documentation, and public-repository
  audit tooling.

## Phase 5

- Added a disabled-by-default TLS LAN server with private-interface binding,
  Argon2id password verification, approved one-time pairing, scoped short-lived
  access credentials, throttling, revocation, and redacted audit events.
- Added the versioned pull/push API, incremental ChangeLog cursors, mutation
  idempotency, tombstones, conflict preservation, and authenticated bounded
  WebSocket events.
- Added desktop device/sync management and focused security, database,
  lifecycle, malformed-request, and loopback integration coverage.

## Phase 4

- Added production sticky sources, controls, persisted opacity/compact state,
  monitor recovery, refined desktop presentation, accessibility semantics, and
  three-window verification.
- Refined all five themes with compact desktop density, focus, menus, dialogs,
  controls, and Obsidian Black polish.

## Phase 3

- Added file-backed Markdown notes, atomic autosave, lifecycle recovery,
  search, archive/Trash, main Notes UI, and dedicated Quick Note window.
- Added WAV PCM recording through `record`, WinMM playback, metadata,
  associations, lifecycle safety, and focused tests.

## Phase 2

- Added immutable task, tag, hierarchy, filter, horizon, and typed-error models.
- Added transactional Drift repositories, revisions, change-log writes,
  lifecycle policies, retention, search, and stable ordering.
- Replaced the workspace placeholder with localized Today, Upcoming, Projects,
  Completed, Archive, Trash, Settings, task editor, and keyboard navigation.
- Added typed Today snapshots and main-coordinated sticky create/complete/reopen.
- Added deterministic unit, database, IPC, and English/Turkish widget tests.
- Completed hierarchy rename/move/reorder/restore controls and keyboard paths.
- Completed the compact task editor fields, selected-task shortcuts, filters,
  and typed localized mutation errors.
- Added the two-engine native sticky create/complete/restart smoke with
  AppDatabase-open instrumentation and sanitized evidence.

## 0.1.0 - 2026-07-19

- Added the Flutter Windows Phase 1 foundation.
- Added portable vault validation, Drift v1 schema, repositories, and tests.
- Added resumable eight-step onboarding and five tokenized themes.
- Added typed multi-window IPC, geometry persistence, and sticky shell.
- Added English/Turkish localization and privacy/security documentation.
